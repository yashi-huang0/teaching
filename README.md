# Yashi Huang — academic website

Static site. `index.html` is self-contained; `Yashi_Huang_CV.pdf` is linked from the page.

Deployed via GitHub Pages (Settings → Pages → Deploy from branch `main`, folder `/`).
